<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash Screen theme. -->
    <style name="Theme.Splash" parent="Theme.MaterialComponents.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen_layer</item>
        <item name="android:statusBarColor">@color/puerto_rico</item>
    </style>

    <!--    <style name="Theme.Splash" parent="Theme.SplashScreen">-->
    <!--        <item name="windowSplashScreenBackground">@color/puerto_rico</item>-->
    <!--        <item name="windowSplashScreenAnimatedIcon">@drawable/dodear_portal</item>-->
    <!--        <item name="postSplashScreenTheme">@style/Theme.SolidApp</item>-->
    <!--    </style>-->

    <!-- Base application theme. -->
    <style name="Theme.SolidApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/puerto_rico</item>
        <item name="colorPrimaryVariant">@color/puerto_rico</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

</resources>